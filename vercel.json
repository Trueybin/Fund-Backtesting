{
  "version": 2,
  "installCommand": "corepack enable pnpm && corepack prepare pnpm@11.7.0 --activate && cd frontend && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable pnpm && cd frontend && pnpm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.py": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
