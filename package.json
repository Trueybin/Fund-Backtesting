{
  "name": "fund-backtesting",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "cd frontend && pnpm install --frozen-lockfile && pnpm run build"
  }
}
